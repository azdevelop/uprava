front_hello:
    pattern:  /
    defaults: { _controller: FrontBundle:Default:index }


front_page:
    pattern:  /page/{page}/{locale}
    defaults: { _controller: FrontBundle:Default:page, locale: locale }
    
front_post:
    pattern:  /post/{post}/{locale}
    defaults: { _controller: FrontBundle:Default:post, locale: locale }
front_navigation:
    pattern:  /navigation/{name}
    defaults: { _controller: FrontBundle:Default:navigation, name:top }
