{% extends 'FrontBundle::layout.html.twig' %}

{% block content -%}
<h1>{{category.title}}</h1>
    {{ include(
            'FrontBundle:Article:list.html.twig',
            { 'posts': posts }
        ) }}

{% endblock %}